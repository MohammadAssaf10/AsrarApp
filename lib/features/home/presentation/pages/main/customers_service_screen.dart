import 'package:flutter/material.dart';

class CustomersServiceScreen extends StatelessWidget {
  const CustomersServiceScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("Customers Service"),
      ),
    );
  }
}
